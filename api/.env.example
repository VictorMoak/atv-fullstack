# URI de conexão do MongoDB Atlas (Network Access: 0.0.0.0/0 para aula)
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/nome-do-banco

# URL do front publicado (usada no CORS)
FRONTEND_URL=http://localhost:5500

# A plataforma de deploy define PORT automaticamente em produção
PORT=3000
